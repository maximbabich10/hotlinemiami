# 🔐 Руководство по авторизации через Telegram бота

## Обзор системы

Новая архитектура бота включает систему авторизации пользователей, где каждый риелтор регистрируется со своим номером телефона и авторизуется на CIAN под своим аккаунтом.

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Telegram Bot                            │
│  (telegram-bot.js)                                          │
│  - Обрабатывает команды пользователей                       │
│  - Управляет регистрацией и авторизацией                    │
│  - Запускает рассылку для каждого пользователя             │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│                     Database Module                         │
│  (database.js)                                              │
│  - SQLite база данных                                       │
│  - Хранит данные пользователей                              │
│  - Отслеживает сессии и статистику                          │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────┐
│                     CIAN Mailer                             │
│  (cian-mailer.js)                                           │
│  - Автоматизация работы с CIAN                              │
│  - Использует номер телефона из БД                          │
│  - Отправляет уведомления через notifier                    │
└─────────────────────────────────────────────────────────────┘
```

## База данных

### Таблица `users`

Хранит информацию о зарегистрированных пользователях:

| Поле | Тип | Описание |
|------|-----|----------|
| `telegram_id` | INTEGER (PK) | Telegram ID пользователя |
| `phone_number` | TEXT | Номер телефона для входа на CIAN |
| `cian_login` | TEXT | Email для входа (опционально) |
| `cian_password` | TEXT | Пароль (опционально, зашифрован) |
| `is_active` | INTEGER | Активен ли пользователь (1/0) |
| `created_at` | DATETIME | Дата регистрации |
| `updated_at` | DATETIME | Дата последнего обновления |

### Таблица `sessions`

Отслеживает сессии рассылки:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER (PK) | ID сессии |
| `telegram_id` | INTEGER (FK) | ID пользователя |
| `started_at` | DATETIME | Время начала |
| `ended_at` | DATETIME | Время окончания |
| `ads_processed` | INTEGER | Количество обработанных объявлений |
| `status` | TEXT | Статус (running/completed/failed) |

## Процесс регистрации

### Шаг 1: Команда `/start`

Пользователь отправляет `/start` боту:

```
Пользователь: /start

Бот: 
🤖 CIAN Telegram Bot

Добро пожаловать! Этот бот автоматизирует рассылку сообщений на CIAN.

❗️ Для начала работы необходимо пройти регистрацию.

Используйте команду /register для регистрации.
```

### Шаг 2: Команда `/register`

Пользователь начинает регистрацию:

```
Пользователь: /register

Бот:
📝 РЕГИСТРАЦИЯ

Для работы с ботом необходимо указать ваш номер телефона, 
который используется для входа на CIAN.

📱 Введите номер телефона в формате: 9771234567
(10 цифр без +7 или 8)

Отправьте /cancel для отмены.
```

### Шаг 3: Ввод номера телефона

Пользователь вводит свой номер:

```
Пользователь: 9771234567

Бот:
✅ РЕГИСТРАЦИЯ ЗАВЕРШЕНА!

Ваш номер телефона сохранен: +79771234567

Теперь вы можете использовать бота для рассылки на CIAN.

При запуске рассылки бот автоматически авторизуется 
под вашим аккаунтом.

Нажмите ▶️ Запустить рассылку для начала работы.
```

## Процесс запуска рассылки

### Шаг 1: Нажатие кнопки "Запустить рассылку"

Пользователь нажимает кнопку **▶️ Запустить рассылку**

### Шаг 2: Авторизация на CIAN

Бот:
1. Извлекает номер телефона пользователя из БД
2. Открывает браузер и переходит на CIAN
3. Вводит номер телефона
4. Запрашивает код подтверждения

```
Бот:
📲 КОД ПОДТВЕРЖДЕНИЯ

На ваш номер отправлен код. 
Пожалуйста, введите код из SMS:
```

### Шаг 3: Ввод кода подтверждения

Пользователь получает SMS с кодом и вводит его:

```
Пользователь: 1234

Бот:
✅ Код получен: 1234

Продолжаю авторизацию...
```

### Шаг 4: Рассылка

Бот начинает обработку объявлений и отправляет уведомления:

```
Бот:
📨 Начинаю обработку объявления:
ID: 123456789
📍 Адрес: Москва, ул. Ленина, д. 1
💰 Цена: 5 000 000 ₽

...

✅ Сообщение введено для объявления:
ID: 123456789
```

### Шаг 5: Завершение

```
Бот:
✅ РАССЫЛКА ЗАВЕРШЕНА!

📊 Обработано объявлений: 15
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы с ботом |
| `/register` | Регистрация нового пользователя |
| `/profile` | Просмотр профиля и статистики |
| `/update` | Обновление номера телефона |
| `/cancel` | Отмена текущей операции |

## Кнопки бота

| Кнопка | Описание |
|--------|----------|
| ▶️ Запустить рассылку | Начать автоматическую рассылку |
| ⏹️ Остановить рассылку | Остановить текущую рассылку |
| 📊 Статистика | Посмотреть статистику работы |
| 👤 Профиль | Ваши данные и настройки |
| ℹ️ Помощь | Показать справку |

## Безопасность

### Хранение данных

- **Номер телефона**: Хранится в открытом виде в SQLite (локальная БД)
- **База данных**: Файл `users.db` в корне проекта
- **Доступ**: Только владелец сервера имеет доступ к БД

### Рекомендации

1. **Не храните пароли**: Используйте только авторизацию по SMS
2. **Ограничьте доступ к серверу**: Используйте SSH ключи
3. **Регулярные бэкапы**: Делайте резервные копии `users.db`
4. **HTTPS**: Используйте SSL сертификат для webhook (если используется)

## Миграция с старой версии

Если вы использовали старую версию с `CIAN_PHONE` в `.env`:

1. Удалите `CIAN_PHONE` из `.env` файла
2. Запустите бота: `npm start`
3. Отправьте боту `/register`
4. Введите свой номер телефона

## Пример использования

### Регистрация первого пользователя

```bash
# Запуск бота
npm start

# В Telegram:
/start
/register
9771234567

# Бот ответит:
✅ РЕГИСТРАЦИЯ ЗАВЕРШЕНА!
```

### Запуск рассылки

```bash
# В Telegram нажмите кнопку:
▶️ Запустить рассылку

# Введите код из SMS:
1234

# Бот начнет рассылку
```

### Просмотр статистики

```bash
# В Telegram нажмите кнопку:
📊 Статистика

# Бот покажет:
📊 СТАТИСТИКА

🎯 Всего сессий: 5
📨 Всего обработано объявлений: 75
🕐 Последняя сессия: 22.11.2025, 15:30
🔴 Статус: Рассылка остановлена
```

## Многопользовательский режим

Каждый пользователь работает под своим аккаунтом:

1. **Пользователь A** (ID: 123456):
   - Номер: +79771234567
   - Обработано: 50 объявлений
   
2. **Пользователь B** (ID: 789012):
   - Номер: +79169876543
   - Обработано: 30 объявлений

Данные не пересекаются, каждый видит только свою статистику.

## Развертывание на сервере

### Требования

- Node.js >= 18.0.0
- SQLite3
- 1GB RAM минимум
- 10GB свободного места

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/hotlinemiami.git
cd hotlinemiami

# Установка зависимостей
npm install

# Настройка .env
cp env.example .env
nano .env  # Укажите TELEGRAM_BOT_TOKEN

# Запуск
npm start
```

### Автозапуск (systemd)

Создайте файл `/etc/systemd/system/cian-bot.service`:

```ini
[Unit]
Description=CIAN Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/hotlinemiami
ExecStart=/usr/bin/node telegram-bot.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте:

```bash
sudo systemctl enable cian-bot
sudo systemctl start cian-bot
sudo systemctl status cian-bot
```

## Мониторинг

### Логи

```bash
# Просмотр логов systemd
sudo journalctl -u cian-bot -f

# Или если запущено вручную
tail -f nohup.out
```

### Проверка БД

```bash
# Подключение к БД
sqlite3 users.db

# Просмотр пользователей
SELECT * FROM users;

# Просмотр сессий
SELECT * FROM sessions ORDER BY started_at DESC LIMIT 10;

# Выход
.exit
```

## Troubleshooting

### Ошибка: "Вы не зарегистрированы"

**Решение**: Отправьте боту `/register`

### Ошибка: "Неверный формат номера"

**Решение**: Введите номер в формате `9771234567` (10 цифр без +7 или 8)

### Ошибка: "Ошибка подключения к БД"

**Решение**: 
```bash
# Проверьте права доступа
ls -la users.db

# Если файла нет, он создастся автоматически при первом запуске
rm users.db  # Удалите и перезапустите бота
npm start
```

### Ошибка: "409 Conflict"

**Решение**: Остановите все экземпляры бота, должен работать только один

```bash
# Найти процессы
ps aux | grep telegram-bot

# Убить процессы
kill -9 <PID>

# Перезапустить
npm start
```

## API для разработчиков

### Database Module

```javascript
const Database = require('./database');
const db = new Database();

// Регистрация пользователя
await db.registerUser(telegramId, phoneNumber);

// Получение пользователя
const user = await db.getUser(telegramId);

// Обновление номера
await db.registerUser(telegramId, newPhoneNumber);

// Создание сессии
const session = await db.createSession(telegramId);

// Обновление сессии
await db.updateSession(sessionId, adsProcessed, 'completed');

// Статистика
const stats = await db.getUserStats(telegramId);

// Закрытие БД
await db.close();
```

## Будущие улучшения

- [ ] Шифрование номеров телефонов в БД
- [ ] Поддержка email + пароль (альтернатива SMS)
- [ ] Веб-интерфейс для управления
- [ ] Экспорт статистики в CSV/Excel
- [ ] Уведомления о завершении рассылки
- [ ] Планировщик рассылок (cron)
- [ ] Поддержка нескольких аккаунтов на одного пользователя

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи: `sudo journalctl -u cian-bot -f`
2. Проверьте БД: `sqlite3 users.db "SELECT * FROM users;"`
3. Создайте issue на GitHub
4. Напишите в Telegram: @your_support_username

