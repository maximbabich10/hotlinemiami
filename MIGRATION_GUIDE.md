# 🔄 Руководство по миграции на v2.0

## Что изменилось?

### Версия 1.0 (старая)
- ❌ Один номер телефона в `.env` файле (`CIAN_PHONE`)
- ❌ Все пользователи работают под одним аккаунтом
- ❌ Нет разделения данных между пользователями

### Версия 2.0 (новая)
- ✅ Каждый пользователь регистрируется со своим номером
- ✅ Многопользовательский режим
- ✅ База данных SQLite для хранения пользователей
- ✅ Индивидуальная статистика для каждого пользователя

## Пошаговая миграция

### Шаг 1: Обновление кода

```bash
# Остановите старую версию бота (если запущена)
# Ctrl+C или найдите процесс:
ps aux | grep telegram-bot
kill -9 <PID>

# Обновите код из репозитория
git pull origin main

# Установите новые зависимости
npm install
```

### Шаг 2: Обновление `.env` файла

**Старый `.env`:**
```env
CIAN_PHONE=9771234567
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_ADMIN_ID=792737507
MAX_PAGES=5
MAX_PER_PAGE=10
MIN_PAUSE=15
MAX_PAUSE=25
```

**Новый `.env`:**
```env
TELEGRAM_BOT_TOKEN=your_token
MAX_PAGES=5
MAX_PER_PAGE=10
MIN_PAUSE=3
MAX_PAUSE=5
```

**Что удалить:**
- `CIAN_PHONE` - больше не нужен (каждый пользователь регистрируется отдельно)
- `TELEGRAM_ADMIN_ID` - больше не используется для авторизации

**Что изменить:**
- `MIN_PAUSE` и `MAX_PAUSE` - можно уменьшить для более быстрой работы

### Шаг 3: Запуск новой версии

```bash
npm start
```

### Шаг 4: Регистрация пользователей

Каждый пользователь должен зарегистрироваться:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Отправьте `/register`
4. Введите номер телефона (10 цифр без +7): `9771234567`

**Пример:**
```
Вы: /start

Бот:
🤖 CIAN Telegram Bot

Добро пожаловать! Этот бот автоматизирует рассылку сообщений на CIAN.

❗️ Для начала работы необходимо пройти регистрацию.

Используйте команду /register для регистрации.

---

Вы: /register

Бот:
📝 РЕГИСТРАЦИЯ

Для работы с ботом необходимо указать ваш номер телефона, 
который используется для входа на CIAN.

📱 Введите номер телефона в формате: 9771234567
(10 цифр без +7 или 8)

Отправьте /cancel для отмены.

---

Вы: 9771234567

Бот:
✅ РЕГИСТРАЦИЯ ЗАВЕРШЕНА!

Ваш номер телефона сохранен: +79771234567

Теперь вы можете использовать бота для рассылки на CIAN.
```

### Шаг 5: Проверка работы

1. Нажмите **▶️ Запустить рассылку**
2. Введите код из SMS
3. Бот начнет рассылку

## Часто задаваемые вопросы (FAQ)

### Q: Сохранятся ли мои обработанные объявления?

**A:** Да, файл `processed_ads.txt` остается без изменений. Все ранее обработанные объявления будут пропущены.

### Q: Могу ли я использовать несколько аккаунтов CIAN?

**A:** Да! Каждый пользователь Telegram может зарегистрировать свой номер телефона и работать под своим аккаунтом CIAN.

### Q: Что делать, если я ошибся при вводе номера?

**A:** Используйте команду `/update` для обновления номера телефона.

### Q: Где хранятся мои данные?

**A:** Все данные хранятся локально в файле `users.db` (SQLite база данных) на вашем сервере.

### Q: Безопасно ли хранить номер телефона в БД?

**A:** Да, база данных хранится локально на вашем сервере. Никто кроме вас не имеет к ней доступа. В будущих версиях планируется добавить шифрование.

### Q: Можно ли вернуться к старой версии?

**A:** Да, но не рекомендуется. Старая версия больше не поддерживается. Если вам нужна старая версия:

```bash
git checkout v1.0  # Переключиться на старую версию
npm install
```

### Q: Что делать с `TELEGRAM_ADMIN_ID`?

**A:** Можете удалить из `.env`. Теперь доступ к боту имеют все зарегистрированные пользователи. Если нужно ограничить доступ, используйте белый список в коде.

## Обратная совместимость

### Файлы, которые остаются без изменений:
- ✅ `processed_ads.txt` - список обработанных объявлений
- ✅ `.env` - настройки (с небольшими изменениями)
- ✅ `package.json` - зависимости (добавлена только `sqlite3`)

### Новые файлы:
- ✅ `database.js` - модуль работы с БД
- ✅ `users.db` - база данных пользователей (создается автоматически)
- ✅ `AUTH_GUIDE.md` - руководство по авторизации
- ✅ `MIGRATION_GUIDE.md` - это руководство

## Проверка миграции

После миграции проверьте:

1. **База данных создана:**
   ```bash
   ls -la users.db
   ```

2. **Пользователь зарегистрирован:**
   ```bash
   sqlite3 users.db "SELECT * FROM users;"
   ```

3. **Бот работает:**
   ```bash
   # Проверьте логи
   tail -f nohup.out
   
   # Или если используете systemd
   sudo journalctl -u cian-bot -f
   ```

4. **Рассылка запускается:**
   - Отправьте боту команду `/start`
   - Нажмите **▶️ Запустить рассылку**
   - Введите код из SMS
   - Проверьте, что бот начал обработку объявлений

## Откат изменений

Если что-то пошло не так, вы можете откатить изменения:

```bash
# Остановите бота
kill -9 $(ps aux | grep telegram-bot | awk '{print $2}')

# Откатите изменения
git reset --hard HEAD~1

# Восстановите зависимости
npm install

# Запустите старую версию
npm start
```

**ВАЖНО:** Перед откатом сделайте резервную копию:
```bash
cp .env .env.backup
cp processed_ads.txt processed_ads.txt.backup
```

## Поддержка

Если у вас возникли проблемы при миграции:

1. Проверьте логи: `tail -f nohup.out`
2. Проверьте БД: `sqlite3 users.db "SELECT * FROM users;"`
3. Создайте issue на GitHub
4. Напишите в Telegram: @your_support_username

## Дополнительные ресурсы

- [AUTH_GUIDE.md](./AUTH_GUIDE.md) - Подробное руководство по авторизации
- [README.md](./README.md) - Главная документация
- [QUICKSTART_RU.md](./QUICKSTART_RU.md) - Быстрый старт

---

**Удачной миграции! 🚀**

