# 🤖 CIAN Telegram Bot - Автоматическая рассылка сообщений

Telegram бот для автоматизации рассылки сообщений на CIAN (сайт недвижимости).

## ✨ Возможности

- ✅ **Полная автоматизация** - авторизация, поиск, отправка
- ✅ **Управление через Telegram** - запуск рассылки по нажатию кнопки
- ✅ **Применение фильтров** - "От собственника" + "Без долей"
- ✅ **Без дублей** - сохраняет ID обработанных объявлений
- ✅ **Защита от капчи** - медленная скорость, антидетект браузер
- ✅ **4 варианта текста** - случайный выбор для уникальности
- ✅ **Мониторинг в реальном времени** - отчеты о прогрессе в Telegram

## 🚀 Быстрый старт

### 1. Установка Node.js

Скачайте и установите Node.js 18+ с [nodejs.org](https://nodejs.org/)

### 2. Клонирование проекта

```bash
git clone <your-repo-url>
cd hotlinemiami
```

### 3. Установка зависимостей

```bash
npm install
```

### 4. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 5. Получение вашего Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Скопируйте ваш ID (число, например `123456789`)

### 6. Настройка `.env` файла

Скопируйте пример конфигурации:

```bash
cp env.example .env
```

Откройте файл `.env` и заполните:

```env
# Настройки для CIAN
CIAN_EMAIL=your_email@mail.ru
CIAN_PASSWORD=your_password

# Настройки Telegram бота
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_ADMIN_ID=123456789

# Настройки рассылки (можно оставить по умолчанию)
MAX_PAGES=5
MAX_PER_PAGE=10
MIN_PAUSE=15
MAX_PAUSE=25

# Опционально: прокси (формат http://логин:пароль@хост:порт)
PROXY_URL=http://user:pass@host:port
```

### 7. Запуск бота

```bash
npm run dev
# или
npm start
```

Вы увидите сообщение:

```
🤖 Telegram бот запущен...
📧 CIAN Email: your_email@mail.ru
🔑 Admin ID: 123456789
✅ Ожидаю команды...
```

### 8. Использование

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Нажмите кнопку **"▶️ Запустить рассылку"**
4. Бот начнет работу и будет присылать обновления о прогрессе

## 📋 Команды и кнопки

- **▶️ Запустить рассылку** - Начать автоматическую рассылку
- **⏹️ Остановить рассылку** - Остановить текущую рассылку
- **📊 Статистика** - Посмотреть количество обработанных объявлений
- **⚙️ Настройки** - Показать текущие настройки бота
- **ℹ️ Помощь** - Показать справку

## ⚙️ Настройки

Все настройки находятся в файле `.env`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `MAX_PAGES` | Количество страниц для обработки | 5 |
| `MAX_PER_PAGE` | Максимум объявлений на странице | 10 |
| `MIN_PAUSE` | Минимальная пауза между объявлениями (сек) | 15 |
| `MAX_PAUSE` | Максимальная пауза между объявлениями (сек) | 25 |
| `PROXY_URL` | Прокси для браузера (`http://user:pass@ip:port`) | — |

**Рекомендуемая скорость:** ~140 сообщений/час (защита от капчи)

## 📁 Структура проекта

```
hotlinemiami/
├── telegram-bot.js      # Telegram бот (главный файл)
├── cian-mailer.js       # Автоматизация CIAN
├── package.json         # Зависимости Node.js
├── .env                 # Настройки (НЕ коммитить!)
├── env.example          # Пример настроек
├── processed_ads.txt    # База обработанных объявлений
├── README_RU.md         # Документация на русском
└── .gitignore           # Игнорируемые файлы
```

## 🔒 Безопасность

- ✅ Файл `.env` автоматически игнорируется Git (не попадет в репозиторий)
- ✅ Только указанный `TELEGRAM_ADMIN_ID` может управлять ботом
- ✅ Пароли и токены хранятся локально

## 🛠️ Как это работает

1. **Авторизация** - Бот входит в ваш аккаунт CIAN
2. **Применение фильтров** - Открывает страницу поиска и применяет фильтры через UI
3. **Поиск объявлений** - Находит все карточки с кнопкой "Написать"
4. **Обработка объявлений**:
   - Проверяет, не обработано ли ранее (по ID)
   - Кликает "Написать"
   - Открывает iframe чата
   - Проверяет наличие истории (если есть - пропускает)
   - Вводит текст (один из 4 вариантов)
   - Сохраняет ID в `processed_ads.txt`
   - Делает паузу 15-25 секунд
5. **Переход на следующую страницу** - Обрабатывает до `MAX_PAGES` страниц

## ⚠️ Важно

- ❗ Бот **НЕ отправляет** сообщения автоматически - только **вводит текст**
- ❗ Можно **останавливать и запускать** снова - продолжит с места остановки
- ❗ При капче - пропускает объявление и продолжает работу
- ❗ Все обработанные ID сохраняются в `processed_ads.txt`

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте версию Node.js (должна быть 18+)
node --version

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Ошибка авторизации на CIAN

- Проверьте правильность email/пароля в `.env`
- Убедитесь, что аккаунт не заблокирован на CIAN
- Попробуйте войти вручную в браузере

### Бот не отвечает в Telegram

- Проверьте правильность `TELEGRAM_BOT_TOKEN` в `.env`
- Убедитесь, что бот запущен (`npm start`)
- Проверьте, что ваш ID совпадает с `TELEGRAM_ADMIN_ID`

### Браузер не открывается

```bash
# Установите Chromium вручную
npx puppeteer browsers install chrome
```

## 📊 Логи и отладка

- **Консоль** - основные логи выводятся в терминал
- **Скриншоты** - сохраняются при ошибках (`error_*.png`)
- **processed_ads.txt** - список всех обработанных объявлений

## 🔄 Миграция с Python версии

Если вы использовали старую Python версию:

1. Старые файлы (`bot.py`, `cian_mailer.py`) можно удалить
2. Файл `processed_ads.txt` совместим - данные сохранятся
3. Настройки из `config.env` перенесите в `.env`

## 📝 Лицензия

MIT

## 🤝 Поддержка

Если возникли вопросы или проблемы - создайте Issue в репозитории.

---

**Удачной рассылки! 🚀**

