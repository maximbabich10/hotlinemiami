# 🧪 Тестирование системы авторизации

## Цель тестирования

Убедиться, что система авторизации работает корректно:
- ✅ Регистрация новых пользователей
- ✅ Хранение данных в БД
- ✅ Авторизация на CIAN с номером из БД
- ✅ Индивидуальная статистика
- ✅ Многопользовательский режим

## Тест 1: Регистрация первого пользователя

### Шаги:
1. Запустите бота: `npm start`
2. Откройте бота в Telegram
3. Отправьте `/start`
4. Отправьте `/register`
5. Введите номер телефона: `9771234567`

### Ожидаемый результат:
```
✅ РЕГИСТРАЦИЯ ЗАВЕРШЕНА!

Ваш номер телефона сохранен: +79771234567
```

### Проверка в БД:
```bash
sqlite3 users.db "SELECT * FROM users;"
```

Должна быть запись:
```
792737507|9771234567|||1|2025-11-22 15:30:00|2025-11-22 15:30:00
```

## Тест 2: Попытка повторной регистрации

### Шаги:
1. Отправьте `/register` еще раз

### Ожидаемый результат:
```
✅ Вы уже зарегистрированы!

Используйте /profile для просмотра данных или /update для обновления.
```

## Тест 3: Просмотр профиля

### Шаги:
1. Отправьте `/profile`
2. Или нажмите кнопку **👤 Профиль**

### Ожидаемый результат:
```
👤 ВАШ ПРОФИЛЬ

📱 Телефон: +7 (977) ***-**-67
🆔 Telegram ID: 792737507
📅 Дата регистрации: 22.11.2025

📊 СТАТИСТИКА:
🎯 Всего сессий: 0
📨 Всего обработано объявлений: 0
🕐 Последняя сессия: Нет данных
```

## Тест 4: Обновление номера телефона

### Шаги:
1. Отправьте `/update`
2. Введите новый номер: `9169876543`

### Ожидаемый результат:
```
✅ Номер телефона обновлен: +79169876543
```

### Проверка в БД:
```bash
sqlite3 users.db "SELECT phone_number FROM users WHERE telegram_id=792737507;"
```

Должно быть: `9169876543`

## Тест 5: Запуск рассылки с новым номером

### Шаги:
1. Нажмите **▶️ Запустить рассылку**
2. Дождитесь запроса кода
3. Проверьте, что код запрашивается на новый номер (+79169876543)

### Ожидаемый результат:
```
📲 КОД ПОДТВЕРЖДЕНИЯ

На ваш номер отправлен код. 
Пожалуйста, введите код из SMS:
```

### Проверка:
- SMS должна прийти на номер +79169876543
- Введите код и убедитесь, что авторизация прошла успешно

## Тест 6: Создание сессии

### Шаги:
1. Запустите рассылку
2. Дождитесь начала обработки объявлений
3. Остановите рассылку или дождитесь завершения

### Проверка в БД:
```bash
sqlite3 users.db "SELECT * FROM sessions WHERE telegram_id=792737507;"
```

Должна быть запись:
```
1|792737507|2025-11-22 15:45:00|2025-11-22 15:50:00|15|completed
```

## Тест 7: Статистика после рассылки

### Шаги:
1. Нажмите **📊 Статистика**

### Ожидаемый результат:
```
📊 СТАТИСТИКА

🎯 Всего сессий: 1
📨 Всего обработано объявлений: 15
🕐 Последняя сессия: 22.11.2025, 15:50
🔴 Статус: Рассылка остановлена
```

## Тест 8: Регистрация второго пользователя

### Шаги:
1. Попросите друга открыть бота
2. Друг отправляет `/start`
3. Друг отправляет `/register`
4. Друг вводит свой номер: `9261234567`

### Проверка в БД:
```bash
sqlite3 users.db "SELECT telegram_id, phone_number FROM users;"
```

Должно быть две записи:
```
792737507|9169876543
7375071931|9261234567
```

## Тест 9: Независимость данных пользователей

### Шаги:
1. Первый пользователь (вы) запускает рассылку
2. Второй пользователь (друг) смотрит свою статистику

### Ожидаемый результат:
- У первого пользователя: статистика с предыдущих запусков
- У второго пользователя: статистика пустая (0 сессий, 0 объявлений)

## Тест 10: Отмена регистрации

### Шаги:
1. Создайте нового пользователя (третий аккаунт Telegram)
2. Отправьте `/register`
3. Отправьте `/cancel` вместо номера

### Ожидаемый результат:
```
❌ Операция отменена.
```

### Проверка в БД:
```bash
sqlite3 users.db "SELECT COUNT(*) FROM users;"
```

Должно быть: `2` (третий пользователь не добавлен)

## Тест 11: Валидация номера телефона

### Тест 11.1: Слишком короткий номер
```
Вы: 977123456 (9 цифр)

Бот:
❌ Неверный формат номера.

Введите 10 цифр без +7 или 8.
Например: 9771234567
```

### Тест 11.2: Слишком длинный номер
```
Вы: 97712345678 (11 цифр)

Бот:
❌ Неверный формат номера.
```

### Тест 11.3: Буквы в номере
```
Вы: 977abc4567

Бот:
❌ Неверный формат номера.
```

### Тест 11.4: Номер с +7
```
Вы: +79771234567

Бот:
❌ Неверный формат номера.
```

### Тест 11.5: Корректный номер
```
Вы: 9771234567

Бот:
✅ РЕГИСТРАЦИЯ ЗАВЕРШЕНА!
```

## Тест 12: Попытка запуска без регистрации

### Шаги:
1. Создайте нового пользователя (четвертый аккаунт Telegram)
2. Отправьте `/start`
3. Нажмите **▶️ Запустить рассылку** (не регистрируясь)

### Ожидаемый результат:
```
❌ Вы не зарегистрированы.

Используйте /register для регистрации.
```

## Тест 13: Проверка notifier

### Шаги:
1. Запустите рассылку
2. Дождитесь начала обработки объявлений

### Ожидаемый результат:
Для каждого объявления должны приходить сообщения:
```
📨 Начинаю обработку объявления:
ID: 123456789
📍 Адрес: Москва, ул. Ленина, д. 1
💰 Цена: 5 000 000 ₽

✅ Сообщение введено для объявления:
ID: 123456789
```

## Тест 14: Graceful shutdown

### Шаги:
1. Запустите бота: `npm start`
2. Нажмите `Ctrl+C`

### Ожидаемый результат:
```
🛑 Получен сигнал остановки...
✅ Бот остановлен
```

### Проверка:
- Браузер должен закрыться
- Соединение с БД должно закрыться
- Процесс должен завершиться корректно

## Тест 15: Бэкап и восстановление БД

### Шаги:
1. Создайте бэкап:
   ```bash
   cp users.db users.db.backup
   ```

2. Удалите БД:
   ```bash
   rm users.db
   ```

3. Запустите бота:
   ```bash
   npm start
   ```

4. Проверьте, что БД создана заново:
   ```bash
   ls -la users.db
   ```

5. Восстановите бэкап:
   ```bash
   cp users.db.backup users.db
   ```

6. Проверьте, что данные восстановлены:
   ```bash
   sqlite3 users.db "SELECT * FROM users;"
   ```

## Результаты тестирования

| Тест | Статус | Примечания |
|------|--------|------------|
| 1. Регистрация первого пользователя | ⬜ | |
| 2. Попытка повторной регистрации | ⬜ | |
| 3. Просмотр профиля | ⬜ | |
| 4. Обновление номера | ⬜ | |
| 5. Запуск рассылки с новым номером | ⬜ | |
| 6. Создание сессии | ⬜ | |
| 7. Статистика после рассылки | ⬜ | |
| 8. Регистрация второго пользователя | ⬜ | |
| 9. Независимость данных | ⬜ | |
| 10. Отмена регистрации | ⬜ | |
| 11. Валидация номера | ⬜ | |
| 12. Запуск без регистрации | ⬜ | |
| 13. Проверка notifier | ⬜ | |
| 14. Graceful shutdown | ⬜ | |
| 15. Бэкап и восстановление | ⬜ | |

## Полезные команды для отладки

### Просмотр всех пользователей
```bash
sqlite3 users.db "SELECT telegram_id, phone_number, created_at FROM users;"
```

### Просмотр всех сессий
```bash
sqlite3 users.db "SELECT * FROM sessions ORDER BY started_at DESC;"
```

### Статистика по пользователю
```bash
sqlite3 users.db "
SELECT 
    u.telegram_id,
    u.phone_number,
    COUNT(s.id) as total_sessions,
    SUM(s.ads_processed) as total_ads
FROM users u
LEFT JOIN sessions s ON u.telegram_id = s.telegram_id
WHERE u.telegram_id = 792737507
GROUP BY u.telegram_id;
"
```

### Удаление пользователя (для тестирования)
```bash
sqlite3 users.db "DELETE FROM users WHERE telegram_id = 792737507;"
```

### Очистка всех данных (для тестирования)
```bash
sqlite3 users.db "DELETE FROM users; DELETE FROM sessions;"
```

## Критерии успеха

Система считается работающей, если:
- ✅ Все 15 тестов пройдены успешно
- ✅ Данные корректно сохраняются в БД
- ✅ Каждый пользователь видит только свои данные
- ✅ Авторизация на CIAN работает с номером из БД
- ✅ Статистика обновляется после каждой сессии
- ✅ Нет ошибок в логах

---

**Удачного тестирования! 🧪**

